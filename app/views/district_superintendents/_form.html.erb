<%= form_for(@district_superintendent) do |f| %>
  <% if @district_superintendent.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@district_superintendent.errors.count, "error") %> prohibited this district_superintendent from being saved:</h2>

      <ul>
      <% @district_superintendent.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :district_id %><br>
    <%= f.number_field :district_id %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
